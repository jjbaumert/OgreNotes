/target
frontend/dist/
frontend/target/
tests/load/target/
tests/rag-eval/target/
# npm installs (e.g. scripts/frontend-doctor); package.json +
# package-lock.json are tracked, node_modules is not.
node_modules/
# .claude — track shared agent definitions (.claude/agents/) so they ride
# with the repo; ignore user-/machine-specific state.
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.idea
.env
.aws-test-config.env

# security sweep verification log — locally meaningful, not for git
design/.security-sweep-log.md

# evolution analysis tool — meta-tooling that reads ~/.claude session
# data; regenerable artifacts, not product code. Untracked by design.
scripts/evolution/
# Long-form narrative docs generated by scripts/evolution/run_all.sh
# (briefing, agent suite, CRDT explainer, build retrospective).
OGRENOTES-*.md

# private local notes not for the repo
private_local/
scratchpad-artifacts/
